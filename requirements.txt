# dashboard/requirements.txt
# Streamlit Cloud reads this because the main file is dashboard/app.py.
# The dashboard calls the Cloud Run API for predictions — it does NOT
# need the ML training stack (optbinning, shap, xgboost) installed here.
# Keep this file as lean as possible to avoid dependency conflicts.

streamlit>=1.32.0
plotly>=5.20.0
pandas>=2.1.0
numpy>=1.26.0,<2.0
requests>=2.31.0
python-dotenv>=1.0.0
